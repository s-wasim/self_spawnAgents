"""{{ tool_name }} — generated by Code Builder

Purpose: {{ purpose }}
Risk tier: {{ risk_tier }}
Network required: {{ network_required }}
"""
from __future__ import annotations

import json
from typing import Any


INPUT_SCHEMA: dict = json.loads({{ input_schema_json | tojson }})
OUTPUT_SCHEMA: dict = json.loads({{ output_schema_json | tojson }})


def run(payload: dict[str, Any]) -> dict[str, Any]:
    """Tool entry point. Replace with real logic."""
    # Placeholder: echo the validated payload keys.
    return {"echoed_keys": sorted(payload.keys())}
